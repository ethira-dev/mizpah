[advisories]
# Keep in sync with reasons in deny.toml — cargo-audit reads `.cargo/audit.toml`.
ignore = [
  # ratatui 0.29 → paste (unmaintained proc-macro)
  "RUSTSEC-2024-0436",
  # ratatui 0.29 → lru 0.12 (unsound IterMut); fixed in lru >=0.16.3
  "RUSTSEC-2026-0002",
]
